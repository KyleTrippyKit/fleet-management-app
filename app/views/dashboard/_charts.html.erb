<%# app/views/dashboard/_charts.html.erb %>
<canvas id="poChart" data-pos="<%= @pos_by_month.to_json %>"></canvas>
<script>
  // Chart.js implementation
  new Chart(document.getElementById('poChart'), {
    type: 'line',
    data: { labels: @months, datasets: [{ data: @counts }] }
  });
</script>