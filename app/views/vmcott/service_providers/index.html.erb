<div class="container-fluid py-4">
  <div class="d-flex justify-content-between align-items-center mb-3">
    <div>
      <h1 class="h3 mb-1">Service Providers</h1>
      <p class="text-muted mb-0">VMCOTT-managed providers (internal workshops + contractors)</p>
    </div>

    <%= link_to "New Provider", new_vmcott_service_provider_path, class: "btn btn-primary" %>
  </div>

  <div class="card shadow-sm mb-3">
    <div class="card-body">
      <%= form_with url: vmcott_service_providers_path, method: :get, local: true, class: "row g-2 align-items-end" do %>
        <div class="col-md-4">
          <label class="form-label small fw-bold">Search</label>
          <%= text_field_tag :q, params[:q], class: "form-control", placeholder: "Search by provider name..." %>
        </div>

        <div class="col-md-3">
          <label class="form-label small fw-bold">Type</label>
          <%= select_tag :type,
                options_for_select([["All", ""], ["Internal Workshop", "internal_workshop"], ["External Contractor", "external_contractor"]], params[:type].to_s),
                class: "form-select" %>
        </div>

        <div class="col-md-2">
          <label class="form-label small fw-bold">Status</label>
          <%= select_tag :active,
                options_for_select([["All", ""], ["Active", "1"], ["Inactive", "0"]], params[:active].to_s),
                class: "form-select" %>
        </div>

        <div class="col-md-3 d-flex gap-2">
          <%= submit_tag "Apply", class: "btn btn-outline-primary w-100" %>
          <%= link_to "Reset", vmcott_service_providers_path, class: "btn btn-outline-secondary w-100" %>
        </div>
      <% end %>
    </div>
  </div>

  <% providers = @service_providers %>
  <% if params[:type].present? %>
    <% providers = providers.where(provider_type: params[:type]) %>
  <% end %>
  <% if params[:active].present? %>
    <% providers = providers.where(is_active: params[:active] == "1") %>
  <% end %>

  <div class="card shadow-sm">
    <div class="card-body p-0">
      <div class="table-responsive">
        <table class="table table-hover mb-0">
          <thead class="table-light">
            <tr>
              <th>Name</th>
              <th>Type</th>
              <th>Contact</th>
              <th>Phone</th>
              <th>Email</th>
              <th>Status</th>
              <th class="text-end">Actions</th>
            </tr>
          </thead>
          <tbody>
            <% if providers.any? %>
              <% providers.each do |sp| %>
                <tr>
                  <td class="fw-semibold"><%= sp.name %></td>
                  <td>
                    <% if sp.provider_type == "internal_workshop" %>
                      <span class="badge bg-info text-dark">Internal Workshop</span>
                    <% else %>
                      <span class="badge bg-secondary">External Contractor</span>
                    <% end %>
                  </td>
                  <td><%= sp.contact_name.presence || "—" %></td>
                  <td><%= sp.phone.presence || "—" %></td>
                  <td><%= sp.email.presence || "—" %></td>
                  <td>
                    <% if sp.is_active %>
                      <span class="badge bg-success">Active</span>
                    <% else %>
                      <span class="badge bg-danger">Inactive</span>
                    <% end %>
                  </td>
                  <td class="text-end">
                    <div class="btn-group btn-group-sm">
                      <%= link_to "Edit", edit_vmcott_service_provider_path(sp), class: "btn btn-outline-warning" %>
                      <%= link_to "Delete",
                            vmcott_service_provider_path(sp),
                            data: { turbo_method: :delete, turbo_confirm: "Delete #{sp.name}?" },
                            class: "btn btn-outline-danger" %>
                    </div>
                  </td>
                </tr>
              <% end %>
            <% else %>
              <tr>
                <td colspan="7" class="text-center text-muted py-4">No service providers found.</td>
              </tr>
            <% end %>
          </tbody>
        </table>
      </div>
    </div>
  </div>
</div>
