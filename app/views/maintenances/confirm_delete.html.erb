<div class="container py-4">
  <h3>Confirm Delete Maintenance Record</h3>

  <div class="card mb-4 shadow-sm">
    <div class="card-body">
      <p>Are you sure you want to delete this maintenance record?</p>

      <ul>
        <li><strong>Date:</strong> <%= @maintenance.date %></li>
        <li><strong>Next Due:</strong> <%= @maintenance.next_due_date || "N/A" %></li>
        <li><strong>Type:</strong> <%= @maintenance.service_type %></li>
        <li><strong>Status:</strong> <%= @maintenance.status %></li>
        <li><strong>Notes:</strong> <%= @maintenance.notes.presence || "-" %></li>
      </ul>

      <div class="mt-3">
        <%= link_to "Delete Permanently", vehicle_maintenance_path(@vehicle, @maintenance),
                    method: :delete, class: "btn btn-danger me-2" %>
        <%= link_to "Cancel", vehicle_path(@vehicle), class: "btn btn-secondary" %>
      </div>
    </div>
  </div>
</div>
