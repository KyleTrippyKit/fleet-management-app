# Maintenance Tasks fixture - matches actual schema
one:
  maintenance: one
  assigned_to_id: <%= ActiveRecord::FixtureSet.identify(:mechanic) %>
  name: Fix brakes

two:
  maintenance: one
  assigned_to_id: <%= ActiveRecord::FixtureSet.identify(:mechanic) %>
  name: Change oil
